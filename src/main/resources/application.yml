spring:
   datasource:
      url: jdbc:mysql://localhost:3306/mvc-app?createDatabaseIfNotExist=true&useSSL=false
      username: root
      password:
      driverClassName: com.mysql.cj.jdbc.Driver
   jpa:
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      show-sql: true
      hibernate.ddl-auto: update
      open-in-view: true